package com.rumpus.rumpus.config;

import com.rumpus.common.Rumpus;

public class Config extends Rumpus {

    // SQL
    // protected static final String SET_USERS_QUERY = "SELECT users.username, users.password, user.email, user.auth_id from users INNER JOIN user ON user.username=users.username WHERE user.username = ?"; // keeping this in case we want to use with join
    // protected static final String SET_USERS_QUERY = "SELECT user.username, user.password, user.enabled user WHERE user.username = ?";
    
}
